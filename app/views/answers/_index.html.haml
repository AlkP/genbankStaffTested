%h1 
  = link_to image_tag('plus.png', border: 0, size: '36x36'), 
            new_answer_path + '?id=' + @question.id.to_s,
            class: 'answer-plus',
            remote: true,
            title: t('.new_answer')
%br
%table.table.table-striped
  %thead
    = render 'answers/answer_title'
  %tbody.tbody-question
    = render @answers